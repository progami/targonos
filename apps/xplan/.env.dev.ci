NODE_ENV=production
PORT=3008
HOST=0.0.0.0

# Database URL - override in CI secrets
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/portal_db?schema=xplan

# Auth - Portal integration (CI placeholder)
NEXT_PUBLIC_APP_URL=https://dev-os.targonglobal.com/xplan
NEXTAUTH_URL=https://dev-os.targonglobal.com/xplan
PORTAL_AUTH_URL=https://dev-os.targonglobal.com

# SECURITY: These secrets MUST be overridden in CI environment variables
# Do not commit real secrets here - these are placeholder values for CI
NEXTAUTH_SECRET=${NEXTAUTH_SECRET:-OVERRIDE_IN_CI_SECRETS}
PORTAL_AUTH_SECRET=${PORTAL_AUTH_SECRET:-OVERRIDE_IN_CI_SECRETS}

COOKIE_DOMAIN=.targonglobal.com

NEXT_PUBLIC_PORTAL_AUTH_URL=https://dev-os.targonglobal.com

BASE_PATH=/xplan
NEXT_PUBLIC_BASE_PATH=/xplan
BASE_URL=https://dev-os.targonglobal.com

# Needed for strategy assignee directory (users with app access to xplan)
# Example: postgresql://portal_auth:***@localhost:5432/portal_db?schema=auth
# PORTAL_DB_URL=postgresql://postgres:postgres@localhost:5432/portal_db?schema=auth

# Optional: override xplan super admins (defaults to jarrar@targonglobal.com)
# XPLAN_SUPER_ADMIN_EMAILS=jarrar@targonglobal.com
